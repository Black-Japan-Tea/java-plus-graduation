@namespace("ru.practicum.ewm.stats.avro")
protocol UserActionsProtocol {
    enum ActionTypeAvro { VIEW, REGISTER, LIKE }

    record UserActionAvro {
        long userId;
        long eventId;
        ActionTypeAvro actionType;
        @logicalType("timestamp-millis")
        long timestamp;
    }
}
